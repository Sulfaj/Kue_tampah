// Formatter harga Rupiah—ubah locale atau currency bila menargetkan pasar lain.
export const formatCurrency = (value: number) =>
  new Intl.NumberFormat('id-ID', {
    style: 'currency',
    currency: 'IDR',
    minimumFractionDigits: 0,
  }).format(value);
